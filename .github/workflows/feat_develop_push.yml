name: Feature -> Develop CI
on:
  push:
    branches: [ develop ]
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  test:
    uses: ./.github/workflows/test.yml
    needs: checkout
